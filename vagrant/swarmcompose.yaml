version: "3.8"
services:
    web:
        image: img-web
        deploy:
            replicas: 5
        ports:
            - 8080:80
        volumes:
            - "${PROJECT_ROOT}:/var/www/html:ro"
        networks:
            - app-network
        entrypoint: ["./web-ngnix.sh"]
    db:
        image: img-db
        networks:
            - app-network
        environment:
            MYSQL_ROOT_PASSWORD: "${DB_ROOT_PASSWORD}"
networks:
    app-network:
